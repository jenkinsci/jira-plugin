name: SonarCloud analysis

on:
  push:
    branches: [ "master" ]
    tags:
      - 'jira*'
  pull_request:
    branches: [ "master" ]
  workflow_dispatch:

permissions:
  pull-requests: read 

jobs:
  Analysis:
    runs-on: ubuntu-latest
    # Fork PRs don't get SONAR_TOKEN (GitHub withholds secrets from pull_request
    # runs triggered by forks). Those are analyzed separately by
    # sonarcloud-fork-build.yml + sonarcloud-fork-analysis.yml.
    if: github.event_name != 'pull_request' || github.event.pull_request.head.repo.fork == false

    steps:
      - name: Checkout Code
        uses: actions/checkout@v7
        with:
          fetch-depth: 0
          persist-credentials: false

      - uses: actions/setup-java@v5
        with:
          java-version: '21'
          distribution: 'corretto'
          cache: 'maven'

      - name: Run the Maven verify phase
        env:
          SONAR_TOKEN: ${{ secrets.SONAR_TOKEN }}
        run: >
           mvn --batch-mode -Penable-jacoco
           -Dsonar.organization=jenkinsci
           -Dsonar.projectKey=jenkinsci_jira-plugin
           -Dsonar.qualitygate.wait
           test jacoco:report org.sonarsource.scanner.maven:sonar-maven-plugin:5.5.0.6356:sonar

